<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage">

    <ScrollView>
        <Grid ColumnDefinitions="*,*" RowDefinitions="*,*,*,*,*,*" RowSpacing="3">
            
            <Picker Grid.Column="0" Grid.Row="0" x:Name="PickerCategory" SelectedItem="Temperature" Title="Select one category"
                Focused="OnConfigOptionsConvertion">
                <Picker.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>Weight</x:String>
                        <x:String>Lenght</x:String>
                        <x:String>Temperature</x:String>
                    </x:Array>
                </Picker.ItemsSource>
            </Picker>

            <Entry Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" x:Name="EntryNumber" Keyboard="Numeric" 
                    Placeholder="input the number"></Entry>

            <Picker Grid.Column="0" Grid.Row="2" x:Name="PickerOrigin" Title="Origin"></Picker>

            <Picker Grid.Column="1" Grid.Row="2" x:Name="PickerDestination" Title="Destination"></Picker>
            
            <Label x:Name="LabelResult" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="3" Text="Result of convertion"></Label>

            <Button x:Name="ButtonInvertConvertion" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="4" 
                    Text="Invert Convertion" Clicked="ButtonInvertConvertion_Clicked"></Button>

            <Button x:Name="ButtonConvert" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="5" 
                    Text="Convert" Clicked="OnClickedConvert"></Button>
        </Grid>
    </ScrollView>

</ContentPage>
